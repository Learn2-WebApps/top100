# ── 보안 민감 폴더 ──────────────────────────────────────────────────────────
# 강사용 자료 (해설, 채점 기준 등) — 절대 커밋하지 않음
instructor/

# ── 원본 데이터 (대용량, 배포에 불필요) ──────────────────────────────────────
dataset_source/
csv_backup/

# ── 빌드/배포 산출물 ──────────────────────────────────────────────────────────
dist/

# ── 의존성 ────────────────────────────────────────────────────────────────────
node_modules/

# ── 로그 ─────────────────────────────────────────────────────────────────────
*.log
npm-debug.log*

# ── OS / 에디터 ───────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
.vscode/
*.swp

# NOTE: web_data/answer_key.json은 서버 API에서만 읽히므로 포함됩니다.
# 클라이언트 번들이나 public 폴더에 노출되지 않습니다.
# 저장소를 private으로 유지하세요.
